# M3c 收藏与成长规则实施计划

1. 用控制器测试锁定收藏鉴权、幂等、区域隔离、分页和取消行为。
2. 同步 H2/MySQL 的 `user_favorite` 表并实现收藏后端。
3. 接入门店详情收藏状态、Web 收藏按钮和我的收藏页。
4. 用服务与控制器测试锁定成长规则奖励、每日上限、启停和等级配置。
5. 同步 `growth_rule`、`level_config`，移除成长值硬编码并增加后台配置页。
6. 更新文档并运行全量测试、构建和浏览器回归。
