# M4b 预订状态机设计

- 时段用 `reserved_count` 原子占用/释放容量，创建、取消、改期均在事务内完成。
- 自动确认时段创建后状态为 `1已确认`，人工确认时段为 `0待确认`。
- 用户只能取消待确认/已确认且未超过取消截止时间的预订。
- 改期先原子占用新时段，成功后释放旧时段；失败保持原预订不变。
- 自动确认新时段保持已确认，人工确认新时段回到待确认。
- 每次创建、取消、改期都写 `reservation_change_log`，详情返回时间线。
- 商户确认、拒绝、到店和爽约动作在 M5 商户工作台复用同一状态机服务。
