# 大众点评(仿)项目 · 文档差异与待确认清单

> 版本:v0.7
> 最后更新:2026-07-12
> 作用:统一登记当前文档冲突、缺失项和建议拍板顺序

## 1. 使用说明

- 发现冲突先记这里,再组织拍板。
- 状态分三种:`P0` 必须先定、`P1` 尽快定、`P2` 可顺延。
- 拍板后要同步回 `需求文档.md`、`接口设计.md`、`数据库设计.md` 和相关配套文档。

## 2. 差异与缺口清单

| ID | 优先级 | 状态 | 主题 | 当前结论 / 剩余问题 | 后续动作 |
|---|---|---|---|---|---|
| D-01 | P0 | 已解决 | 收藏模型 | 一期统一为 `1店铺 2帖子` | 后续如扩团购/点评收藏,新增枚举值 |
| D-02 | P0 | 已解决 | 预订状态机 | 已补确认/拒绝/改期/提醒/爽约/到店闭环 | 下一步细化字段级 schema |
| D-03 | P0 | 已解决 | 商户端权限 | 已补主账号/店长/核销员/客服运营角色模型 | 后续决定是否开放自定义角色 |
| D-04 | P0 | 已解决 | 区域语义 | 已统一 `request_region` / `data_region` / `preferred_region` | 后续补管理端区域数据权限实现细节 |
| D-05 | P1 | 已解决 | 搜索历史 | 已补 `search_history` 表与接口口径 | 需要补保留上限与清理策略 |
| D-06 | P1 | 已解决 | 推送设备 | 已补 `user_device`、设备注册与 token 生命周期 | 需要补具体推送失败重试策略 |
| D-07 | P1 | 已解决 | 私信举报 | 已补举报对象与接口口径 | 后续补审核工作台处理细节 |
| D-08 | P1 | 已解决 | 运营配置持久化 | 已补 `rank_config`、`operation_activity`、活动资源项模型 | 后续再决定是否扩 AB 实验 |
| D-09 | P1 | 已解决 | 圈子命名 | 对外统一 `group`,数据库内部保留 `circle` | 无 |
| D-10 | P1 | 已解决 | 幂等与限流 | 已补通用 `Idempotency-Key` 作用域、场景限流阈值与库表支撑 | 后续决定是否开放后台可配 |
| D-11 | P2 | 已解决 | 验证码与找回密码 | 已补 `verification_code` 与 `/auth/password/reset` | 后续补供应商切换策略 |
| D-12 | P2 | 已解决 | 数据导出与删除 | 已补隐私中心接口、任务模型、冷静期与匿名化口径 | 后续定下载二次确认方案 |
| D-13 | P2 | 已解决 | 迭代实施计划 | 已补 `M1-M2实施计划与验收清单.md`,可直接用于排期与验收 | 后续按真实人力调工期 |
| D-14 | P2 | 已解决 | 第三方供应商选型 | 已补 `第三方供应商选型表.md`,给出 CN/EU 主方案与接入顺序 | 后续填真实预算数字与供应商联系人 |
| D-15 | P2 | 已解决 | 测试清单 | 已补 `测试清单与验收用例.md`,覆盖验证码、审核、支付回调、WebSocket、隐私中心 | 后续继续往支付 / 预订 / 隐私中心扩自动化覆盖 |
| D-16 | P2 | 已解决 | 上线准备 | 已补 `上线准备清单.md`,覆盖环境、密钥、回调、监控、灰度、回滚 | 后续填执行版环境参数与值班联系人 |
| D-17 | P2 | 已解决 | 预算与采购 | 已补 `预算与采购开通清单.md`,覆盖预算结构、账号开通、域名证书、续费提醒 | 后续填真实预算数字与联系人 |
| D-18 | P2 | 已解决 | 运维值班与故障响应 | 已补 `值班与故障响应手册.md`,覆盖故障分级、值班角色、升级路径、止血与回滚 | 后续填实名值班表与升级树 |
| D-19 | P2 | 已解决 | 自动化测试与发布 | 已补 `自动化测试与发布流水线说明.md`,并已落地 `verify-all.ps1`、`storage-smoke.ps1`、`package-release.ps1`、`deploy-release.ps1`、`rollback-release.ps1` 以及 GitHub Actions `ci/nightly/release/rollback` workflow | 下一步填真实 secrets、目标机参数并做发布 / 回滚演练 |
| D-20 | P2 | 已解决 | 真实执行信息落表 | 已补 `执行版联系人与台账模板.md`,覆盖联系人、预算、证书、回调、值班实名表 | 下一步填入真实执行数据 |

## 3. 下一阶段执行顺序

1. 本机 `scripts/ci/mysql-smoke.ps1` 已于 `2026-07-12` 用临时 `MySQL 8` 实例跑绿；如果后面要复用宿主机 `MySQL80`,先把现有 root 凭证修好。
2. 给现有 GitHub Actions / 目标机补齐 `DEPLOY_*`、`APP_S3_*`、目标环境数据库 / Redis 参数等真实 secrets 与环境参数,演练一次发布 / 回滚。
3. 并行把 `执行版联系人与台账模板.md` 填成真实执行版。

前面的 P0/P1 大坑和 `D-13` 到 `D-20` 都已经收住了。现在不是继续开文档大会的时候,而是该补真实环境、该演练发布回滚、该填执行信息的时候。

## 4. 拍板输出要求

每个差异项拍板后至少要同步下面三样:

- 需求层:补到功能范围或规则描述。
- 接口层:补动作、字段、鉴权、错误码。
- 数据层:补表、字段、枚举、索引。

## 5. 退出条件

当下面三条都满足时,可以认为文档进入“可开工”状态:

- 所有 P0 项已拍板并同步。
- 所有核心流程在 `业务流程与状态机.md` 中已闭环。
- `接口设计.md` 与 `数据库设计.md` 不再出现同字段不同含义的冲突。

当前判断:

- 上述条件已满足。
- 文档已达到“可开始搭项目”的状态。
- 剩余工作主要是执行层填表和工程落地,不再是文档缺口。
