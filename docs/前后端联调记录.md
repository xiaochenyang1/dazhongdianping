# 前后端联调记录

> 最后更新:2026-07-13
> 适用范围:当前仓库**已经落地并且前端有入口**的页面 / 接口
> 先说人话:这份文档证明的是“当前做出来的东西大部分已经联调过了”,不是“整个项目目标态全做完了”

## 1. 文档目的

这份文档专门回答一个问题:

**现在前端和后端到底联调到哪一步了?**

别再拿“文档里规划了很多能力”冒充“代码都联调完了”。当前能确认的是:

- `web` 和 `admin-web` 当前已经落地的主要页面,我已经按真实后端环境跑过一轮。
- 核心主流程已经打通,本地可演示。
- `2026-07-11` 代码已补 `web` 的成长值流水页、头部搜索历史面板 / 清空、`CN / EU` 区域切换,并补齐头部搜索联想 / 热词 / 历史面板切区按当前区域重拉、公开用户主页按当前区域重拉与后端 `reviewCount` 区域统计,且已通过本地 `npm test` / `npm run build` / `./mvnw.cmd test`;但下面“本轮联调环境”的真实后端页面巡检基线仍是 `2026-07-10` 那一轮。
- `2026-07-12` 又补跑了一轮真实后端 Playwright E2E:`scripts/ci/browser-e2e.ps1` 已覆盖写点评时真实图片上传、成长值流水页、管理端审核通过、公开点评互动和游客互动登录后自动续执行。
- `2026-07-13` 已补隐私中心前台和第 4 条真实后端 E2E,覆盖数据导出任务、认证 ZIP 下载、账号删除申请与冷静期撤销。

## 2. 本轮联调环境

| 项目 | 值 |
|---|---|
| 日期 | `2026-07-10` |
| 系统 | `Windows` |
| 后端 | `http://127.0.0.1:8080` |
| C 端前台 | `http://127.0.0.1:5173` |
| 管理端 | `http://127.0.0.1:5174` |
| 数据库 | `MySQL / dzdp_local` |
| 健康检查 | `/actuator/health = UP` |
| 当前有效门店数 | `4` |
| 当前有效点评数 | `13` |
| 当前导入批次数 | `1` |
| 当前成长值流水数 | `6` |

### 2.1 本轮使用的演示账号

| 用途 | 账号 | 密码 |
|---|---|---|
| C 端演示 | `demo.cn@example.com` | `Demo123456` |
| 管理端演示 | `admin` | `admin123456` |

## 3. 本轮联调结论

### 3.1 总结版

- 自动化浏览器冒烟已通过:
  - `scripts/ci/browser-smoke.ps1`: `6/6`
  - `web/e2e/real-backend-flow.spec.ts`: `4/4`(在原有点评 / 用户中心链路外,新增隐私导出下载、删除申请与撤销)
- 真实页面巡检已通过:
  - `web` 当前路由 `11` 个
  - `admin-web` 当前路由 `5` 个
  - 合计 `16/16` 打开成功
- 真实写操作已通过:
  - C 端密码登录、写点评、后台审核通过、点赞、评论、举报
  - 写点评真实图片上传
  - 游客点赞 / 评论 / 举报登录后自动续执行
  - 成长值流水页回看刚提交点评对应的奖励记录
  - 我的资料保存并回滚
  - 绑定手机号到完成
  - 修改密码并用新密码重新登录
  - 管理端门店新增 / 修改 / 删除
  - 管理端成功导入 / 导入失败链路

### 3.2 本轮能负责任地说的话

- **当前已经做出来的主要页面和核心接口,基本都联调过了。**
- **可以拿来本地演示。**
- **不能吹成“整个项目所有目标态功能都联调完了”。**

## 4. 已联调范围

### 4.1 C 端公开浏览与搜索

| 页面 / 动作 | 主要接口 | 结果 | 说明 |
|---|---|---|---|
| 首页 `/` | `GET /api/c/v1/user/me` `GET /api/c/v1/categories` `GET /api/c/v1/cities` `GET /api/c/v1/home/banners` `GET /api/c/v1/home/feed` | 通过 | 页面可打开,真实数据可展示 |
| 商户列表 `/shops?keyword=火锅` | `GET /api/c/v1/shops` | 通过 | 真实返回 `渝里火锅徐汇店` |
| 头部热词 | `GET /api/c/v1/search/hot` | 通过 | 下拉面板可见热词 |
| 头部联想 | `GET /api/c/v1/search/suggest` | 通过 | 输入“火”可拿到联想项 |
| 联想跳列表 | `GET /api/c/v1/search/suggest` `GET /api/c/v1/shops` | 通过 | 点击联想后跳到列表页 |
| 商户详情 `/shops/10001` | `GET /api/c/v1/shops/10001` `GET /api/c/v1/shops/10001/reviews` | 通过 | 基础信息和点评预览正常 |
| 门店点评 `/shops/10001/reviews` | `GET /api/c/v1/shops/10001` `GET /api/c/v1/shops/10001/reviews` | 通过 | 公开点评列表正常 |

### 4.2 C 端认证、点评、用户中心

| 页面 / 动作 | 主要接口 | 结果 | 说明 |
|---|---|---|---|
| 游客访问写点评页 | 登录拦截逻辑 | 通过 | 未登录会弹登录框 |
| 密码登录 | `POST /api/c/v1/auth/login/password` | 通过 | `demo.cn@example.com` 可正常登录 |
| 写点评页 `/shops/10001/reviews/new` | `GET /api/c/v1/shops/10001` | 通过 | 页面已接真实门店数据 |
| 提交点评 | `POST /api/c/v1/reviews` | 通过 | 真实提交成功 |
| 管理端审核通过后回看 | `POST /api/admin/v1/audit/tasks/{id}/pass` `GET /api/c/v1/reviews/{reviewId}` | 通过 | 新点评能转公开可见 |
| 点评详情 `/reviews/13` | `GET /api/c/v1/reviews/13` `GET /api/c/v1/reviews/13/comments` | 通过 | 当前本机联调点评 `id=13` 可打开 |
| 点赞 | `POST /api/c/v1/reviews/13/like` | 通过 | 点赞数已落库 |
| 评论 | `POST /api/c/v1/reviews/13/comments` | 通过 | 评论已落库 |
| 举报 | `POST /api/c/v1/reviews/13/report` | 通过 | 举报记录和审核任务已落库 |
| 编辑点评页 `/reviews/13/edit` | `GET /api/c/v1/user/reviews/13` | 通过 | 页面可打开并回填 |
| 我的点评 `/user/reviews` | `GET /api/c/v1/user/reviews` | 通过 | 我的点评列表正常 |
| 我的点评详情 `/user/reviews/13` | `GET /api/c/v1/user/reviews/13` | 通过 | 审核状态正常显示 |
| 我的资料 `/user/profile` | `GET /api/c/v1/user/me` | 通过 | 资料页可正常展示 |
| 保存资料 | `PUT /api/c/v1/user/profile` | 通过 | 已实测保存,随后已回滚原始签名 |
| 绑定手机号 | `POST /api/c/v1/auth/send-code` `POST /api/c/v1/user/bind` | 通过 | `2026-07-12` Playwright H2 E2E 已实测绑定成功 |
| 修改密码 | `PUT /api/c/v1/user/password` `POST /api/c/v1/auth/login/password` | 通过 | `2026-07-12` Playwright H2 E2E 已实测改密并用新密码重新登录 |
| 成长值流水 `/user/growth-records` | `GET /api/c/v1/user/me` `GET /api/c/v1/user/growth/records` | 通过 | `2026-07-12` Playwright H2 E2E 已回看刚提交点评奖励记录 |
| 公开用户主页 `/users/9001` | `GET /api/c/v1/user/9001` | 通过 | 页面正常展示 |
| 隐私中心 `/user/privacy` | `GET /api/c/v1/privacy/overview` `POST/GET /api/c/v1/privacy/export-tasks` `GET /api/c/v1/privacy/export-tasks/{id}/download` `POST /api/c/v1/privacy/delete-tasks` `POST /api/c/v1/privacy/delete-tasks/{id}/cancel` | 通过 | 已实测创建导出、下载 ZIP、提交删除申请并在冷静期撤销 |

### 4.3 管理端

| 页面 / 动作 | 主要接口 | 结果 | 说明 |
|---|---|---|---|
| 登录页 `/login` | 无需后端即可打开 | 通过 | 页面可打开 |
| 管理员登录 | `POST /api/admin/v1/auth/login` | 通过 | `admin / admin123456` 可登录 |
| 控制台 `/dashboard` | `GET /api/admin/v1/menus` `GET /api/admin/v1/shops` `GET /api/admin/v1/import/batches` | 通过 | 概览页正常 |
| 商户管理 `/data/shops` | `GET /api/admin/v1/shops` `GET /api/c/v1/categories` `GET /api/c/v1/cities` `GET /api/c/v1/cities/{id}/areas` | 通过 | 列表和元数据都正常 |
| 新建门店 | `POST /api/admin/v1/shops` | 通过 | 已实测 |
| 修改门店 | `PUT /api/admin/v1/shops/{shopId}` | 通过 | 已实测 |
| 删除门店 | `DELETE /api/admin/v1/shops/{shopId}` | 通过 | 已实测并确认删除后不留活动门店残留 |
| 点评审核 `/audit/reviews` | `GET /api/admin/v1/audit/tasks` | 通过 | 页面正常显示待审任务 |
| 审核通过点评 | `POST /api/admin/v1/audit/tasks/{taskId}/pass` | 通过 | 已实测 |
| 种子导入页 `/data/import` | `GET /api/admin/v1/import/batches` | 通过 | 页面可打开 |
| 成功导入 | `POST /api/admin/v1/import/shops` | 通过 | `2026-07-12` Playwright H2 E2E 已实测成功导入 1 条 |
| 故意失败导入 | `POST /api/admin/v1/import/shops` | 通过 | 已生成失败批次和错误文件 |

## 5. 本轮联调留下的真实证据

这些不是嘴上说“测过了”,是真的在当前本机库里留下了痕迹:

- 新增了 `1` 条联调点评:
  - `review.id = 13`
  - 内容前缀: `真实后端 E2E 点评`
  - 当前状态: `audit_status=1`
  - 当前点赞数: `1`
  - 当前评论数: `1`
- 这条点评对应的互动已落库:
  - `review_like` 有记录
  - `review_comment` 有记录
  - `review_report` 有记录
  - `audit_task` 有“审核通过”和“举报待复核”两类记录
- 新增了 `1` 条失败导入批次:
  - `import_batch.id = 1`
  - `file_name = frontend-import-fail-1783669951886.json`
  - `status = 2`
  - `success = 0`
  - `failed = 1`
  - `error_file = local-storage/import-errors/import-batch-1-errors.json`
- 门店 CRUD 联调时创建的临时门店已清理:
  - `shop.name like '联调门店_%' and is_deleted=0` 当前为 `0`

## 6. 本轮没联调或故意没做到底的

这些地方别硬往“已全部联调完”里塞:

- `GET /api/c/v1/user/growth/records`
  - 这页已经纳入 `2026-07-12` 的真实后端 Playwright E2E;当前欠的是单独手工巡检截图式记录,不是页面/接口没通。
- `B` 端前台
  - 目前只有 `/api/b/v1/*` 最小只读接口骨架,没有独立 `b-web` 页面,这份文档不覆盖它。
- 目标态的 `M3-M7`
  - 交易、预订、完整商户台、完整运营后台等目标态功能,当前大部分本来就还没落代码,更谈不上联调。

## 7. 当前最短演示路径

如果你只是想自己点一遍,按这个顺序最省劲:

1. 打开 `http://127.0.0.1:5173`
2. 首页顶部搜索框输入“火”,确认联想能出来
3. 进入“渝里火锅徐汇店”,看详情和公开点评
4. 用 `demo.cn@example.com / Demo123456` 登录
5. 打开“我的点评”和“我的资料”
6. 打开 `http://127.0.0.1:5174/login`
7. 用 `admin / admin123456` 登录后台
8. 看“商户管理”“点评审核”“种子导入”

## 8. 当前口径怎么说才不扯

### 8.1 可以这么说

- 当前仓库**已经做出来并且前端有入口**的主要页面和核心接口,已经联调过一轮。
- C 端和管理端主流程已经能跑,可以本地演示。

### 8.2 不要这么说

- “整个项目所有接口和页面都已经全部联调完毕了”
- “所有文档里的能力都已经测试完成了”
- “已经达到最终上线态了”

## 9. 一句话结论

**当前已落地的 C 端和管理端核心闭环,已经基本联调通了;能演示,但不是全项目目标态 100% 完工。**
