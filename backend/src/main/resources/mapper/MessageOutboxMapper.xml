<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tuowei.dazhongdianping.module.messaging.mapper.MessageOutboxMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO message_outbox(region, topic, payload, status, published_at)
        VALUES(#{region}, #{topic}, #{payload}, 1, CURRENT_TIMESTAMP)
    </insert>

    <select id="selectById" resultType="com.tuowei.dazhongdianping.module.messaging.model.MessageOutboxRow">
        SELECT id, region, topic, payload, status, created_at, published_at
        FROM message_outbox
        WHERE id=#{id}
    </select>

</mapper>
