<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tuowei.dazhongdianping.module.review.mapper.ReviewTranslationMapper">

    <sql id="translationColumns">
        id, review_id, region, user_id, target_lang, content, created_at, updated_at
    </sql>

    <select id="selectByUser" resultType="com.tuowei.dazhongdianping.module.review.model.ReviewTranslationRow">
        SELECT <include refid="translationColumns"/>
        FROM review_translation
        WHERE review_id = #{reviewId}
          AND user_id = #{userId}
          AND target_lang = #{targetLang}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO review_translation(review_id, region, user_id, target_lang, content)
        VALUES(#{reviewId}, #{region}, #{userId}, #{targetLang}, #{content})
    </insert>

    <update id="updateContent">
        UPDATE review_translation
        SET content = #{content},
            updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>

    <select id="selectById" resultType="com.tuowei.dazhongdianping.module.review.model.ReviewTranslationRow">
        SELECT <include refid="translationColumns"/>
        FROM review_translation
        WHERE id = #{id}
    </select>

    <select id="selectByReview" resultType="com.tuowei.dazhongdianping.module.review.model.ReviewTranslationRow">
        SELECT <include refid="translationColumns"/>
        FROM review_translation
        WHERE review_id = #{reviewId}
          AND region = #{region}
        <if test="targetLang != null and targetLang != ''">
            AND target_lang = #{targetLang}
        </if>
        ORDER BY id DESC
    </select>
</mapper>
