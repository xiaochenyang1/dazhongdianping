import type{PageResult}from'./browse';export interface ReservationSlot{slotId:number;startTime:string;endTime:string;capacity:number;remainingCount:number;confirmMode:number;confirmModeText:string;cancelBeforeMinutes:number;available:boolean;closedReason:string}export interface SlotResponse{shop:{id:number;name?:string};date:string;list:ReservationSlot[]}export interface ReservationPayload{shopId:number;slotId?:number;reserveTime?:string;peopleCount:number;contactName:string;contactPhone:string;remark:string}export interface ReservationTimeline{actionType:number;actionText:string;operatorType:number;operatorText:string;remark:string;createdAt:string}export interface Reservation{ id:number;reservationNo:string;shop:{id:number;name:string;coverImage:string;address:string};slotId:number;reserveTime:string;peopleCount:number;contactName:string;contactPhone:string;remark:string;status:number;statusText:string;confirmMode:number;confirmModeText:string;rescheduleCount:number;canCancel:boolean;canReschedule:boolean;timeline?:ReservationTimeline[]}export type ReservationPage=PageResult<Reservation>
